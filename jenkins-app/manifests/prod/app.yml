apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-app
  labels:
    name: prod-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: prod-app
  minReadySeconds: 60
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        name: prod-app
    spec:
      containers:
      - name: prod-app
        image: gcr.io/khaleelterraform/app/prod/jenkinsapp:latest
        imagePullPolicy: Always

---
kind: Service
apiVersion: v1
metadata:
  name: prod-app
spec:
  selector:
    name: prod-app
  ports:
    - name: port1
      protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer

---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: prod-app
spec:
  maxReplicas: 3
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prod-app
  targetCPUUtilizationPercentage: 80
